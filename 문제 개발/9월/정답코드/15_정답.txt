#include <cstdio>

int x;

int main() {
  scanf("%i", &x);// %i는 8진수, 10진수, 16진수를 다 입력받을 수 있다
  printf("%d\n", x);
  return 0;
} 

// 알아야 할 것
// %i는 8진수, 10진수, 16진수를 다 입력받을 수 있다!

// 다른 방법 
// 문자열로 입력받고 직접 계산해서 값을 구할 수 있다.
